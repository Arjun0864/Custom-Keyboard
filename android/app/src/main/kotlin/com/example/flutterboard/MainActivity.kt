package com.example.flutterboard

import io.flutter.embedding.android.FlutterActivity

// Launcher activity — opens the Flutter settings screen.
// The keyboard itself is pure native (FlutterBoardIME), no Flutter involved.
class MainActivity : FlutterActivity()
